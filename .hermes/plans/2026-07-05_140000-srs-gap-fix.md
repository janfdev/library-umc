# SRS Gap Implementation Plan — Library UMC

**Date:** 2026-07-05
**Gaps:** FR-03 (Advance Search), FR-07 (Report Export), FR-08 (Reservation Notification), NFR-03 (HTTPS)

---

## Phase 1: FR-03 — Advance Search (Publisher filter)

**Status:** ⏳ Pending
**Estimate:** 15 min

### What's missing
Backend bibliography search already supports: title, ISBN, call number, author, year range, GMD, language.
**Only gap:** Publisher filter is commented out as "skip for now — requires join"

### Implementation
```
1. src/modules/bibliography/service/bibliography.service.ts
   - Add LEFT JOIN to publishers table when publisher query param is present
   - Uncomment the publisher filter
```

### Files to modify
- `library-be/src/modules/bibliography/service/bibliography.service.ts`

---

## Phase 2: FR-07 — Report Export (Verify + Add missing exports)

**Status:** ⏳ Pending
**Estimate:** 30 min

### What's missing
Backend already has `GET /reports/loans/export?format=csv|pdf` and `GET /reports/fines/export?format=csv|pdf`.
Need to verify the controller implementation actually generates PDF/CSV.

### Implementation
```
1. Read report.controller.ts to verify export implementations exist
2. If missing -> implement using PDFKit for PDF, csv-writer for CSV
3. If existing -> just verify TypeScript compiles
```

### Files to check
- `library-be/src/modules/report/controller/report.controller.ts`

---

## Phase 3: FR-08 — Reservation Notification

**Status:** ⏳ Pending
**Estimate:** 20 min

### What's missing
When a reservation is fulfilled (auto-fulfill when book returned), email notification should be sent to the member.

### Implementation
```
1. src/modules/loan/service/loan.service.ts
   - In approveReturnRequest, after fulfillNextReservation():
     add NotificationService.sendReservationFulfilled() 
```

### Files to modify
- `library-be/src/modules/loan/service/loan.service.ts`
- `library-be/src/modules/notification/service/notification.service.ts` (add method)

---

## Phase 4: NFR-03 — HTTPS Redirect

**Status:** ⏳ Pending
**Estimate:** 5 min

### What's missing
No HTTP→HTTPS redirect middleware. SRS requires HTTPS.

### Implementation
```
1. src/index.ts — add HTTPS redirect middleware before all routes
```

### Files to modify
- `library-be/src/index.ts`

---

## Verification

- [ ] `npx tsc --noEmit` → 0 errors
- [ ] All existing tests still pass
- [ ] Each feature verified individually
